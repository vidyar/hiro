language: php

php:
  - 5.4

before_script:
  - mkdir -p shippable/testresults

script:
  - phpunit  --log-junit shippable/testresults/junit.xml tests --bootstrap tests/bootstrap.php

notifications:
  email: false
